---
description: Kit visual @ui — lee COMPONENTES.md antes de escanear .vue
alwaysApply: true
---

# Kit visual (`@ui`)

Antes de abrir `.vue` en `front/src/shared/ui/`, lee `front/src/shared/ui/COMPONENTES.md`.

Ahí está el índice, cómo armar vistas (hero + `.wrap`) y qué piezas son solo demo de `/admin/ui`.
